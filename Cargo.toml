[package]
name = "wifi-tui"
version = "0.1.0"
edition = "2021"
description = "A nice TUI to scan and connect WiFi networks on netplan/networkd systems"

[dependencies]
ratatui = "0.29"

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
